# Gemfile
# This includes all of the gems to be used in our project
# Ensure that they are downloaded by running "bundle install"
# If bundler is not installed, run "gem install bundle"

# The source for gems to be downloaded.  This should almost
# always be rubygems.org unless you have a specific reason not to do so
# (e.g., some companies have private gem sources local to the system
# for security reasons).
source "https://rubygems.org"

# List of all gems to be included in this project
gem "minitest"
gem "flamegraph"
gem "stackprof"
